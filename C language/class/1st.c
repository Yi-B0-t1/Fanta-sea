#include <stdio.h>
int main()
{
    double s = 1, a, b, c;
    for (a = 1, b = 2; a < 10.0, b < 1025; a++, b *= 2)
    {
        s *= 1 / a;
    }
    printf("the sum is__%.20lf__\n", s);
    printf("the sum is__%e__\n", b);
    int x = 1, y = 1, k = 1;
    printf("the number is__%.20lf__", 2(1 - y * y));
    printf("the number is__%.20lf__", -4 * x * y);
    printf("the number is__%.20lf__", 2 * (2 - x * x));
    printf("__%d__%d__%d__", i, j, k);
    return 0;
}